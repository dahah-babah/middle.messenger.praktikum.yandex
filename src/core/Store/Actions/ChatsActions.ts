import Store from 'src/core/Store/Store'

const store = new Store()

export const chatsActions = {
  // setChats: (data: IUser) => {
  //   const state = store.getState()
  //   const user = state.user ?? {}
  //
  //   store.set('chats', { ...user, ...data })
  // },
}
